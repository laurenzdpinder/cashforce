const {
  readBuyerbyId,
} = require('./Buyer');

const {
  readOrders,
} = require('./Order');

const {
  readProviderbyId,
} = require('./Provider');

module.exports = {
  readBuyerbyId,
  readOrders,
  readProviderbyId,
};
